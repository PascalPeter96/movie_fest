import 'package:flutter/material.dart';
import 'package:movie_fest/models/movie.dart';

class LargeDetails extends StatelessWidget {
  final Result? movie;
  const LargeDetails({Key? key, this.movie}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
